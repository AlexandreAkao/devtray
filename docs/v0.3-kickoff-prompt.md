# v0.3 Kickoff Prompt

Paste this into a fresh Claude Code session inside `~/www/personal/devtray`. The auto-memory (`devtray-state.md`) will load and provide the rest of the context.

---

Quero começar o v0.3 do DevTray. v0.2 acabou de shippar (memória `devtray-state.md` tem o estado atual; tag v0.2.0 no main, 7 tools funcionando).

v0.3 adiciona o `DevTrayStorage` package — primeiro entry de persistência. Escopo mínimo do spec: tabela `tool_usage` (tool_id, used_at) pra desbloquear o "frequent tools" footer chips e ranking por uso. Veja spec geral em `docs/superpowers/specs/2026-05-20-devtray-design.md` seções "Release phasing" + "Data and persistence" + "Schema". Template do plano é o v0.2 em `docs/superpowers/plans/2026-05-21-devtray-v0.2-tools.md`.

Antes de escrever spec/plano, faz brainstorm rápido sobre:

1. Schema do v0.3 — só `tool_usage` ou já inclui `migrations` table (GRDB.DatabaseMigrator precisa)?
2. GRDB.swift como o spec diz, ou avaliar SQLite.swift / outra?
3. Instrumentação: cada tool grava manualmente em `makeView`, ou o `Tool` protocol/Registry intercepta automático?
4. UI do footer chips (top 3 por frequência total? últimos 7 dias? misto?). Onde os chips entram no `PopoverRoot` (substitui o "DevTray vX.Y.Z" ou fica ao lado?).

Depois: writing-plans → branch `feat/v0.3-storage` → subagent-driven-development.

**Bonus cleanup task no início do plano** (gotchas listados em `devtray-state.md`):

- SwiftPM stub workaround — engine task antes da view task quebra `swift test`. Vale baked no template (stub instrução) ou rearquitetar o Package.swift por tool (só declarar XxxTool target quando há fontes).
- XcodeGen zerando `DevTrayApp.entitlements` toda vez que `bootstrap.sh` roda. Fix: declarar `entitlements.properties` inline no `project.yml`.
- Version string hardcoded em `PopoverRoot.footer` e `AboutTab`. Fix: ler de `Bundle.main.infoDictionary["CFBundleShortVersionString"]`.
- Cache do `xcodebuild` não pegando mudanças em packages SPM locais. Considera default `clean build` em `scripts/test.sh` ou doc.

Faz brainstorm decidindo quais dos 4 entram como pre-work do v0.3 vs ficam para v0.4+.
